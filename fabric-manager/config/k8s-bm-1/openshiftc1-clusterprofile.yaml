apiVersion: netop.mgr/v1
kind: ClusterProfile
metadata:
  name: openshiftc1
  namespace: netop-manager
spec:
  cluster_input:
    profile:
      cni: openshift-4.10-openstack
  selector:
    matchLabels:
      computes: NOVA
      distro: K8S-1.21
      location: ON-PREM
      network-infra: ACI
      pod-network: ACI-CNI
