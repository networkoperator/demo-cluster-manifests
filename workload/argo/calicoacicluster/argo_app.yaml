apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netop-manager
  namespace: netop-manager-system
spec:
  destination:
    namespace: netop-manager
    server: https://kubernetes.default.svc
  project: default
  source:
    path: workload/config/calicoacicluster/
    repoURL: https://github.com/networkoperator/demo-cluster-manifests
    targetRevision: filip
    directory:
      exclude: '*.tar.gz'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
    - Replace=true